# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

dist-desktop/
codex-next-dev.err.log
codex-next-dev.out.log
electron_build.log
electron_build_dir.log
git_log.txt
test_err.txt
test_errors.txt
test_out.txt
unified_utils_tmp_old.ts

# CyberLearn — work in progress, never commit
app/tools/cyberlearn/

# Uploaded transcript files — runtime data, served from disk, not tracked in git
uploads/

# Service-account / credential JSON files — never commit
gen-lang-client-*.json

# Large seed data files — seeded directly to DB, not tracked in git
lib/cyberquiz/seeds/*.json

# Large binary downloads — served from hosting, not tracked in git
public/downloads/*.exe
public/downloads/*.dmg
public/downloads/*.zip

# local dev-server scratch logs
test/devserver.*.log
